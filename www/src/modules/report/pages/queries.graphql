query Reports($start: Int, $limit: Int, $where: JSON) {
  reports(start: $start, limit: $limit, where: $where) {
    title
    featured
    created_at
    excerpt
    type
    attachments {
      url
    }
  }
}
