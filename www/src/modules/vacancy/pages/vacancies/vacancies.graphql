query Vacancies($limit: Int, $where: JSON, $sort: String) {
  vacancies(start: 0, limit: $limit, where: $where, sort: $sort) {
    id
    title
    location
    deadline
    description
    attachment {
      url
    }
  }
}
