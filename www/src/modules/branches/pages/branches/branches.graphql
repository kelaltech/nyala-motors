query Branches {
  branches(sort: "name:ASC") {
    name
    mapCoordinates
    physicalAddress
    dealerTypes {
      dealerType
    }
    workingHours
    phoneNumbers {
      phoneNumber
    }
    emails {
      email
    }
  }
}
