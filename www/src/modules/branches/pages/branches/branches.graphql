query Branches {
  branches(sort: "order:ASC") {
    name
    mapCoordinates
    physicalAddress
    dealerTypes {
      dealerType
    }
    workingHours
    phoneNumbers {
      phoneNumber
    }
    emails {
      email
    }
  }
}
